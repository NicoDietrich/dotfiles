
alias get_song='get_song &'

# Sude such that it works with other aliases
alias sudo='sudo '
alias grep='grep --color'
alias ls='ls --color'

# Copy shortcut with progress bar
alias cpr='rsync -a --progress'

# Direcotry listing
alias ll='ls -lh'
alias la='ls -lah'

# Move around quicker
alias cd2="cd ../.."
alias cd3="cd ../../.."
alias cd4="cd ../../../.."
alias cd5="cd ../../../../.."

# Update
alias sysupd="sudo apt-get update && sudo apt-get upgrade"

# Directory stack
# alias dirs="dirs -v"

# Print
alias homeprint="lp -d MFC5890CN"

alias nemo="neno --no-desktop"
